//**! === Imports === */
import '../style.css';
//** ==== === Header === ==== */
export const headerPlus = (document.querySelector(`#idHeader`).innerHTML = ` 
 <header class="header__container">Hello Header</header>
`);

export default headerPlus;
