//**! === Imports ===  */
import '../style.css';
//** ==== === Main === ==== */
export const mainPlus = (document.querySelector(`#idMain`).innerHTML = ` 
  <main class="main__container"></main>
`);

export default mainPlus;
